
export class UserPreferences {
    language: string; 
    timezone: string;
    notificationOn: boolean;
    promotionsOffersOns: boolean; 

    constructor(data?: any) {
        if (data) {
           
        }
    }
}
